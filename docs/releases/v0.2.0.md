This release consolidates the original 0.1.0 public baseline with the
subsequent real-host verification and release-hardening work. It is still an
early release, but the tested surface is substantially broader and the
remaining native behavior is now an explicit compatibility boundary.

## Battle UI deferment — 2026-08-15

The attempted Gen2 Clean UI battle replacement is not part of the active
release path. Its adapters, presenters, detached renderer, ownership logic,
and battle fixtures were removed and archived at
`docs/archive/battle-ui-deferred-2026-08-15/`. `Gen2BattleState` and
`Gen2BattleTransition` remain native/deferred, with the official host owning
battle timing, input, sound, randomization, semantics, and state transitions.
The battle design goals are retained in `docs/BATTLE_SYSTEM_HANDOFF.md`, but no
replacement architecture is being committed in this release.

The historical battle bullets below describe the removed attempt and must not
be read as current live-launcher support or acceptance proof.

## Pokegear-family deferment — 2026-08-15

The active replacement for `Gen2Pokegear` and `Gen2MapRadio` is disabled. The
official native renderer owns the complete Pokegear flow, including phone,
clock, map/Fly, and radio surfaces. The old adapters, presenters, and fixtures
remain only as inactive reference coverage for a later redesign; they are not
registered as live presenters, V3 preview actions, or production screens.

The Pokédex remains active and received a contained composition refresh: a
stable Gen1 Modern-inspired list/preview arrangement, number-first rows,
seen/owned markers, type badges, palette-aware detached sprites, totals, and
navigation hints. It uses the existing V3 menu contract and does not capture or
reuse the native canvas.

Clean UI settings now include Clean, Dark, High Contrast, Red, Gold, Blue,
Silver, and Crystal palettes. The five new palettes are registered in shared
Core with readable text/paper contrast, and the Mod Menus page follows the
persisted selection on released hosts.

The active Poké Mart V3 path now composes the top-level menu, BUY list, SELL
Pack child, buy/sell quantity states, money, prices, and owned inventory
counts from detached source data. The official transparent `MartMenu` contract
is represented as non-opaque, fixing the live `opacity_mismatch` fallback that
left the native Mart visible. A user walkthrough after sync confirmed Clean UI
ownership; minor Mart bugs remain for follow-up.

The historical Pokegear bullets below describe that inactive reference
implementation and are not current live-launcher support.

The packaged manifest requires host release `0.1.87` or newer and no longer
uses the obsolete experimental manifest flag.

The 0.1.87 release-floor pass also fixes retained gameplay stacks: the
source-owned overworld is preserved as a backdrop while every labelled clean
menu/submenu state is prepared and suppressed through its V3 presenter. This
keeps the drop-in package compatible with the released launcher without
modifying or rebuilding the host.

The post-send-out battle hand-off now marks incomplete battler snapshots as
transient. Core retains the last complete Clean battle frame during that
released-host rebuild window, and the battle renderer keeps the base Pokémon
art visible when an optional animation payload is incomplete. Message-free
battle phases now receive stable stage labels instead of a blank lower dock.

The latest deterministic battle hardening keeps the prior complete Clean frame
across supported child-screen push/pop rebuilds, detaches released-host
animation background registers and hidden-battler state, and fails open when a
required detached source frame is absent. Available trainer/faint displacement
and background state are consumed inside the fixed battle arena; missing move
or damage animations are never represented by a text damage banner. This
remains subject to official launcher verification and does not close the full
battle milestone.

The battle path is now fully detached from native source-canvas capture. The
V3 adapter exports source-timed intro offsets, trainer/faint motion, sprite
scale/true-color metadata, OAM tile-sheet selection/crops/palettes/flips, and
background scroll/palette state. Core composes the field, clean rails, effects,
and fixed lower dock as separate layers, while ownership latches across
rebuilt battle states and supported child stacks. The product does not install
a Gen2 `render.compose` battle renderer.

Host redraws are no longer treated as battle exits: when a refresh lands during
a transient V3 snapshot, the last complete detached frame remains the owner
until a complete battle prepare succeeds. A deterministic product-smoke
regression covers this at a changed viewport; official-launcher verification is
still required. Explicit `battle_finished` extraction is preserved so terminal
cleanup releases ownership instead of retaining a stale battle canvas.

Transform/substitute/minimize override identities fail open when the current
V3 snapshot does not include a replacement sprite descriptor; the clean
presenter reports `battle_picture_override_unavailable` instead of showing a
stale species image. No native frame is used as an animation fallback.

The sync and release scripts use built-in .NET SHA-256 checks so the launcher
archive can be built from the stock Windows PowerShell command path even when
its optional utility module is not auto-loaded.

The shared V3 contract API now supports `all_generations = true` for one
generation-neutral registration covering both Clean UI products; manifests
still declare launcher eligibility explicitly.

The settings shell now uses the V3 session-local option fallback on the
read-only v0.1.86 options surface. Pointer/touch remains implemented as
low-priority V3 groundwork, but its setting is hidden and the runtime path is
disabled by default because click behavior is incomplete across screen
families. The released host includes both `input.pointer` and `mod.input:tap`,
so this can be revisited without a host modification.

## Follow-up status

Battle and the Pokegear family are deferred and are not active 0.2.0
replacement milestones. Party is the first current follow-up, followed by
Trainer Card, Summary, Pack, Pokédex, Save, dialogue, and settings/pointer-touch
polish. Pokegear, Map, and Radio remain native until a separate redesign.
`docs/BATTLE_SYSTEM_HANDOFF.md` records future design goals only; no live
battle walkthrough or battle rewrite is claimed here.

Future map polish will follow Gen1 Modern's extracted native-map graphic
composition. Full Gen2 animation parity remains tracked separately for
background scanline/palette effects and displacement/picture overrides.

This release-status update carries no screenshots or generated Git/release
artifacts, and it does not include commits or host-repository changes.

## Baseline carried forward from v0.1.0

- Stable Gen II battle menus, move selection, message frames, responsive
  battle envelopes, palette-aware sprites, and metadata-rich status cards.
- The battle composition is now built around a fixed 960×540 16:9 stage,
  540×960 phone staging, and a compact lower dock roughly one-third the height
  of the upper field. The wide dock now has a dedicated compact gutter and a
  tested three-to-one stage hierarchy target; it expands only when the
  four-move list and move detail require readable content. Intro/trainer
  slides use detached trainer art, and incomplete OAM snapshots cannot blank
  the clean battlefield.
- Exact-contract presenters for the audited 0.3 families, production Gallery
  integration, fail-open native fallbacks, and headless CI validation.

## Gen2 Clean UI coverage

- The official catalog now enumerates exactly 51 Gen II screen IDs, with 37
  active production presenters, 12 intentional native presenters, and 2
  explicitly deferred battle records. The complete Pokegear family is native;
  the battle transition is also deferred pending a future rewrite.
- The Gen II Copyright splash, Game Freak Presents splash, Gold/Silver intro,
  and Gold title screen remain native/source-owned for the initial release.
  Their experimental V3 raster presenters are not registered for gameplay after
  visual verification exposed incorrect scanline/sprite extraction.
- The credits roll now uses a V3 full-viewport animation presenter with
  source-owned timing, scene art, banner scrolling, and ordered text layers;
  missing extracted art still fails open to the native roll.
- The Game Freak and Gold/Silver intro extraction work remains available only as
  callback-free editor experiments until the native raster seams are proven.
- Egg Hatch now uses a full-viewport V3 animation presenter with source-owned
  beat timing, extracted egg and hatchling art, palette selection, crack frames,
  and normalized shell fragments; incomplete art fails open to native.
- Evolution now uses the canonical V3 animation seam for source-owned
  flash/reveal timing, blackout and palette-aware species art, text beats, and
  expanding light-circle particles; incomplete art/data fail open to native.
- Gold/Silver Intro remains explicitly native after visual verification found
  incorrect horizontal/vertical raster bars in the experimental replacement.
- Shared UI primitives now centralize content-driven sizing, text measurement,
  dialogue reflow, menu growth, safe-area placement, density scaling, and
  portrait/landscape adaptation so long labels are not truncated simply to fit
  a fixed native-sized box.
- Battle status cards preserve the native Gen II arrangement while exposing
  level, HP, gender, conditions, known wild-catch state, player EXP, and
  colored type badges for both active Pokémon. Gender uses the supplied
  16x16 male/female sprite sheet rather than a font glyph.
- Battle move selection supports up to four known moves in a vertical list,
  with a dedicated detail panel for type, Gen II physical/special category,
  PP, power, accuracy, and move description. The portrait command menu keeps
  the native 2x2 input layout.
- Stable battle decision/message frames now reserve one phase-independent
  envelope sized for the four-move state, so opening the command menu or move
  list does not squish the status cards, sprites, or arena. Player status puts
  HP above EXP, and the portrait command menu retains its native 2x2 input
  layout. The pre-battle transition uses a transparent canonical V3 overlay.
- The vendored Core lock records the lifecycle runtime's current hash and size,
  so release packaging cannot silently ship an unlocked runtime change.
- Fixed the live OAM crop-index multi-return regression that could abort an
  intro, move, Poké Ball, or item animation frame and briefly expose native
  battle UI. Missing optional effect sheets now skip only that effect tile;
  the Clean UI battle frame remains active.
- Battle presentation now keeps ownership across transient rebuilt
  `BattleState` snapshots. A stable encounter/screen identity retains the last
  complete Clean frame when a source battler or OAM snapshot is briefly
  incomplete, and releases immediately when the stack leaves battle. This is
  a presentation latch only; host timing, input, audio, and gameplay remain
  authoritative.
- Live battle OAM is exposed as a named V3 `sceneFrame` (with `frameData` kept
  as a compatibility alias), including native-stage dimensions, ordered
  objects, crops, palettes, and battler/background metadata. Optional effects
  are constrained to the arena and cannot overwrite either status rail.
- The active battle visual direction combines three complementary pieces:
  an open diagonal arena/status layout, a compact native 2x2 command dock, and
  a dedicated move-information panel beside the vertical list of up to four
  known moves. The phase-independent envelope remains reserved so menus never
  squish the Pokémon HUD or sprites.
- The battle adapter now enforces the four-move Gen II maximum at the V3
  boundary, mirrors the live `sceneFrame` at the top-level battle model, and
  lets the move detail description reflow within its panel instead of exposing
  native pagination markers or a single-line truncation.
- V3 now exposes an explicit `BATTLE_CLASSIC` 10:9 surface in addition to the
  default 16:9 wide and 9:16 phone envelopes; resolved layout metadata reports
  the selected aspect for Studio previews and alternate presenters.
- Source-owned timing and scanline parity remain authoritative in the host,
  while the clean presenter now covers the full available battle phase
  envelope: faint/progression stats, move-learning completion, post-catch
  naming, and supported battle child-stack ownership. Unknown or unsupported
  child stacks still fail open to native rendering.
- Post-catch nickname entry now uses a Gen1 Modern-style compact keyboard
  rendered by the Core V3 naming surface after the released `ui.naming.grid`
  hook is confirmed; player, rival, and box naming retain their exact Gen2
  source boards.
- Battle intro now carries the released host's player back-pic and opponent
  trainer-class front-pic as generated, palette-aware V3 sprite descriptors;
  Pokémon sprites take over only after their source send-out/slide phases.
- Battle regression coverage verifies that live move/item/Poké Ball OAM frames
  remain detached data inside the canonical V3 battle model, including source
  sheet geometry and OAM objects.
- The provider now fail-opens any V3 presenter missing the canonical schema,
  API version, or presentation kind before it can suppress native rendering.
- Every model-ready production Gallery fixture is now checked against the
  canonical Core V3 validator, keeping editor/catalog examples on the same
  presentation contract as gameplay presenters.
- Release-tool smoke now derives its version, tag, and curated blurb from the
  manifest, protecting future release-note bumps from stale `0.2.0` literals.
- Scaffold verification now requires the manifest-matched curated blurb before
  the local product gate can pass.
- Pokegear now has a responsive smartphone-style shell with a status bar,
  app rail, active-card treatment, and view-specific content region while
  retaining the native directional/A/B control flow.
- Pokegear Map now composes the extracted native 20x18 Johto/Kanto tilemaps
  from the shared gear sheet with per-tile palette selection, selected/current
  landmark overlays, native cursor-sheet artwork, and a safe marker fallback
  when a host asset is not available. Landscape screens use a 16:9 shell,
  portrait screens use a 9:16 shell, Fly presents the map and destinations in
  one responsive surface, and the post-Elite-Four Kanto map uses the same
  production path.
- API V3 now exposes the editor-facing `contract_catalog` capability and
  `cleanUiHost.listContracts(filter)`. Returned descriptors are copied,
  deterministic, and callback-free. The core repository includes a
  deterministic `ui-editor-fixture` example that exercises a panel, dropdown,
  direct dialogue/choice/battle/animation models, named actions, modal response, and
  Gallery metadata as a test target for the
  standalone editor WIP; declarative screen action references are validated
  atomically during registration, focus skips non-actionable label rows, and
  direct menu/dialogue/choice/battle/animation presentation models are supported. Shared
  TextBox, ChoiceBox, Main Menu, Start Menu, and Options now emit the canonical
  V3 model schema. Party and Summary now emit the same schema, including their
  HP/status/EXP and move-detail data. The product registers
 `gen2_shared_dialogue`, `gen2_foundation_menus`, and `gen2_party_menus` as real
 production contracts for editor/catalog inspection. No standalone editor is
 claimed as complete here.
- Gen2 now also publishes `gen2_official_catalog`, a callback-free 51-entry V3
  status inventory preserving every exact host screen ID, support state,
  milestone, and native fallback reason. It is metadata-only and does not
  suppress any native screen.
- Core now validates and renders first-class V3 `device` and `map` models.
  Gen2 Pokegear emits `device` for its smartphone/card views and `map` for
  Map/Fly, including portable shell descriptors, landmark/Fly rows, native
  tilemap geometry, and cursor-sheet metadata. Generalized live animation
  timing and complete child-stack ownership remain documented Core/product
  gaps outside the battle scene-frame path, so unknown children continue to
  fail open instead of using legacy callbacks or misleading placeholder
  banners.
- Product smoke now locks the exact twelve native-by-design records, including
  the complete Pokegear family and the four boot/title screens whose
  experimental raster presenters showed horizontal/vertical corruption. They
  remain native until separately verified source seams exist.
- Core and Studio now validate animation sprite descriptors as a shared V3
  contract: every sprite has an asset path and complete positive placement,
  while crops, flips, and palettes are checked before rendering. The product
  smoke also validates every direct screen published by its V3 catalog.
- Core and Studio now validate panel descriptors as a shared strict V3
  contract: screen/component IDs, dense collections, known fields, nested
  options/fields/footer lists, duplicate IDs, and type-specific payloads are
  checked before export, registration, or rendering. Invalid panel replacement
  attempts remain atomic.
- Pack, Pokegear, and Map Radio now emit the canonical V3 presentation schema,
  with callback-free inventory/device examples in `gen2_inventory_device`.
- Pokedex, Trainer Card, and Save now emit the canonical V3 presentation
  schema, with callback-free progress examples in `gen2_progress_menus`.
- Battle presentation models now emit the canonical V3 schema and are exposed
  through `gen2_battle_preview`. Timed animation models are exposed through
  `gen2_battle_animations` with six callback-free Gallery examples covering
  intro, move, item, experience, level-up, and transition states; live
  source-owned timing remains authoritative, and unsupported child stacks
  remain explicit native/fail-open boundaries.
- The callback-free V3 catalog now includes `gen2_boot_animations` examples for
  the Copyright splash, Game Freak Presents, Gold title screen, and credits roll, with asset-backed
  full-viewport animation models available to the standalone editor WIP.
- The callback-free V3 catalog now includes `gen2_cinematic_animations` with
  asset-backed Egg Hatch, Evolution flash/reveal, and Gold/Silver Intro examples
  for the standalone
  editor WIP.
- The callback-free V3 catalog now includes `gen2_extended_menus` examples for
  naming, storage, marts, mail, clock, and Hall of Fame flows, giving the
  standalone editor broader real-product fixtures to exercise.
- All 41 integrated official production presenter records now enforce the V3
  presentation model at runtime; native-by-design records remain outside that
  boundary.
- The V3 host now advertises `presentation_models` and validates direct
  menu/dialogue/choice/battle/animation models at registration and action-result
  boundaries, preserving the previous valid screen on failure. Ordered model
  collections and selection indices are structurally validated as well, so
  sparse rows, scalar dialogue lines, malformed action data, keyed sprite maps,
  out-of-bounds overlay rectangles, invalid animation labels, and invalid RGBA
  channels fail open before rendering. Studio mirrors the same geometry and
  color validation.
- The shared core now exposes `validateV3`, `measureV3`, `drawV3`, and
  `renderV3` for the standalone Studio embedding bridge, keeping editor
  previews on the same validation, responsive layout, and renderer path as the
  product runtime.
- Battle menu, move-selection, touch-geometry, viewport/font-fit, and
  battle-specific responsive matrices are covered by deterministic checks;
  the current product battle matrix passes 21,921 combinations and the
  Pokegear/MapRadio suite passes 212 checks. The shared core unit suite passes
  275 checks and Studio passes 54 checks, including real V3
  validation/catalog coverage for the
  checked-in editor fixture.
- Pokegear, Map Radio, services, commerce, mail, specialty displays, the Hall
  of Fame viewer, and the Gallery use production model/presenter paths with
  explicit native fallback for unsupported child states.

## Real-host verification since v0.1.0

- The host integration uses the V3 `render.ui.prepare`,
  `screen.render_visible`, and `render.hud` seams around native screen
  composition, allowing Gen2 presenters to prepare a complete replacement
  frame before `screen.render_visible` suppresses the source UI. Bryan's
  v0.1.86 host exposes `mod.options:define/get` but not `mod.options:set`;
  Clean UI now uses the shared V3 session-local fallback for settings edits
  and Reset Defaults on that supported public surface, while native writers
  remain preferred when a later host provides one.
- Bryan's v0.1.86 host also predates `mod.ui.isBuiltinScreen`. Official Gen2
  identity validation treats that predicate as optional: newer hosts still
  receive the additional class check, while v0.1.86 uses the exact stamped
  `screenId`, source registry ownership, and native draw-override guard. This
  keeps official-screen replacement compatible with the released host.
- The same v0.1.86 fallback is covered against all 51 official screen records,
  not only the main-menu fixture. The legacy `screen.render_visible` hook now
  resolves the live `game` from the state object, keeping Party, Battle,
  Pokegear, dialogue, and pointer/touch replacement on one compatibility path.
- Restored Party and Battle overrides on unmodified v0.1.86 launchers. That
  public host predates the newer generated-image helper, so the drop-in mod
  now uses the host's sandboxed graphics loader only for validated generated
  PNG assets. Newer hosts continue to use their source-image facade and cache
  ownership. No `gen1recomp` patch or rebuild is required.
- The V3 runtime now invalidates its completed canvas on `screen.pushed` and
  `screen.popped`, preventing a closed or replaced UI from surviving into the
  next HUD composition pass when the older host skips the newer prepare seam.
- Gold-host menu smoke covers boot/menu, Start Menu, Party, Pack, Pokegear,
  Trainer Card, Pokedex, Options, Save, PC/storage, clock setup, and Fly Map.
- Battle smoke covers trainer intro and sprite scaling, battle Pack refusal and
  return behavior, forced-switch Party behavior, catch flow, native move
  animations, REVIVE, ETHER, faint flow, and forced switching.
- The post-catch nickname path opens the Clean UI naming keyboard and retains
  the entered nickname in the settled party record; full-party catches land in
  the active PC box without disturbing the party.
- Progression smoke covers the native EXP-bar crawl through level-up, move
  learning, evolution completion and B-cancel, summary pages, egg hatch,
  naming, trade-item display, and TAKE-to-bag behavior.
- An ephemeral no-write route replay reaches the Champion and Hall of Fame
  boundary from the untouched Gold slot with no harness teleports, route-loop
  give-ups, or route failures; the source save remains unchanged.
- Save veto behavior and the canonical Hall of Fame writer are guarded by host
  tests so visual verification does not silently mutate the user's save.

## Validation and release hardening

- Scaffold validation is CRLF-safe and checks the exact catalog, presenter
  inventory, contract coverage, and manifest metadata.
- The release archive is rebuilt twice in an isolated copy and checked for
  deterministic hashing, root containment, manifest ordering/version, and
  required vendored-core entries.
- Versioned curated blurbs live in `docs/releases/vX.Y.Z.md`; the release
  workflow now injects the matching blurb, commit range, and archive SHA-256
  into the GitHub release automatically.
- The Gen1 and Gen2 local launcher sync helpers now replace their exact mod
  targets after validating the expected `%APPDATA%/pokemon-love2d/mods`
  parent, preventing stale files from surviving a development sync.
- Local visual-audit captures remain available for testing but are excluded
  from Git; release notes describe verified behavior rather than packaging
  screenshots.

## Deliberate scope boundary

Battle-owned child stacks and some native animation primitives remain native
where replacing them would risk gameplay behavior. The pre-battle transition,
in-battle intro, animation-state surfaces, and Evolution cinematic are
represented by Clean UI, while source timing and unproven child stacks remain
fail-open around those boundaries.
